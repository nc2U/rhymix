apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rhymix.fullname" . }}-test-connection"
  labels:
    {{- include "rhymix.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rhymix.fullname" . }}:{{ .Values.nginx.service.port }}']
  restartPolicy: Never
