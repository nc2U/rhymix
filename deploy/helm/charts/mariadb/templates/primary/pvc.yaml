apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-{{ include "mariadb.primary.fullname" . }}-backup-pvc
spec:
  storageClassName: dump-backup-{{ .Values.global.appMode }}
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Mi
